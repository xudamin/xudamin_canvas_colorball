var Ball = Class.extend({	init: function(paramsJSON){		this.posX = paramsJSON.posX || 0;		this.posY = paramsJSON.posY || 0;		this.controller = paramsJSON.controller;		this.ctx = this.controller.ctx;		this.r = 8;		this.isNeedDestroy = false;		this.dX = this.getRandom(6) - 3;		this.dY = this.getRandom(6) - 3;		this.dR = this.getRandom() + 0.01;		this.color = 'rgba('+ Math.floor(this.getRandom(255)) +','+ Math.floor(this.getRandom(255)) +','+ Math.floor(this.getRandom(255)) +',1)'	},	update: function(){		this.posX += this.dX;		this.posY += this.dY;		this.r -= this.dR;		if(this.r<=0){			this.r = 0.1;			this.isNeedDestroy = true;		}		if(this.posX<0 || this.posX>this.controller.canvas.width){			this.dX = -this.dX;		}		if(this.posY<0 || this.posY>this.controller.canvas.height){			this.dY = -this.dY;		}	},	render: function(){		this.ctx.beginPath();		this.ctx.arc(this.posX,this.posY,this.r,0,Math.PI*2,true);		this.ctx.fillStyle = this.color;		this.ctx.fill();		this.ctx.closePath();	},	getRandom: function(max){		return Math.random()*(max||1);	}});